{{- if and .Values.redis.install.enabled .Values.redis.pdb.enabled }}
{{- if and .Values.redis.pdb.minAvailable .Values.redis.pdb.maxUnavailable }}
{{- fail "redis.pdb: cannot specify both minAvailable and maxUnavailable, choose one" }}
{{- end }}
{{- if not (or .Values.redis.pdb.minAvailable .Values.redis.pdb.maxUnavailable) }}
{{- fail "redis.pdb: must specify either minAvailable or maxUnavailable when pdb is enabled" }}
{{- end }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "adaptive.redis.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "adaptive.labels" . | nindent 4 }}
    {{- include "adaptive.redis.selectorLabels" . | nindent 4 }}
spec:
  {{- if .Values.redis.pdb.minAvailable }}
  minAvailable: {{ .Values.redis.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.redis.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.redis.pdb.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "adaptive.redis.selectorLabels" . | nindent 6 }}
{{- end }}
