{{- if .Values.controlPlane.pdb.enabled }}
{{- if and .Values.controlPlane.pdb.minAvailable .Values.controlPlane.pdb.maxUnavailable }}
{{- fail "controlPlane.pdb: cannot specify both minAvailable and maxUnavailable, choose one" }}
{{- end }}
{{- if not (or .Values.controlPlane.pdb.minAvailable .Values.controlPlane.pdb.maxUnavailable) }}
{{- fail "controlPlane.pdb: must specify either minAvailable or maxUnavailable when pdb is enabled" }}
{{- end }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "adaptive.controlPlane.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "adaptive.labels" . | nindent 4 }}
    {{- include "adaptive.controlPlane.selectorLabels" . | nindent 4 }}
spec:
  {{- if .Values.controlPlane.pdb.minAvailable }}
  minAvailable: {{ .Values.controlPlane.pdb.minAvailable }}
  {{- end }}
  {{- if .Values.controlPlane.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.controlPlane.pdb.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "adaptive.controlPlane.selectorLabels" . | nindent 6 }}
{{- end }}
